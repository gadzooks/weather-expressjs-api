meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Response contains HTML", function() {
    expect(res.body).to.include('Hello from the TypeScript world');
  });

  test("Response time is acceptable", function() {
    expect(res.responseTime).to.be.below(1000);
  });
}

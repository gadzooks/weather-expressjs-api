# Visual Crossing API Key
# Get your API key from https://www.visualcrossing.com/
VC_API_KEY=your-api-key-here

# Cache TTL in hours
CACHE_TTL_HOURS=3

# Comma-separated list of allowed CORS origins
# Development example with common React dev server ports:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Production example:
# ALLOWED_ORIGINS=https://weather.weekendwanderings.com

# QA example:
# ALLOWED_ORIGINS=https://qa-weather-react.onrender.com

# For AWS SAM deployments:
# Before deploying, export VC_API_KEY in your shell:
#   export VC_API_KEY=your-actual-api-key
# Then run deployment commands:
#   yarn sam:deploy        # Deploy to production
#   yarn sam:deploy:qa     # Deploy to QA
#   yarn sam:deploy:dev    # Deploy to dev
